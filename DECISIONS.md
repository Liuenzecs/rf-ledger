# DECISIONS.md

## 目的

本文档记录 RF
Ledger 当前已确认的长期设计决策、取舍原因与后续影响。短期实现细节不必全部写入；只有会影响后续开发方向或接口稳定性的事项，才应该沉淀到这里。

## 1. 产品范围保持“本地优先、完全离线”

- 决策：应用继续保持本地优先和完全离线，不引入云同步、远程后端或外部 API 依赖。
- 原因：记账数据偏私密；当前目标是先把桌面端可用性、可靠性和数据掌控感做好。
- 影响：所有读写、统计、备份、恢复都围绕本地 SQLite 和本地文件系统设计。

## 2. `account` 字段暂时承担“支付方式”语义

- 决策：本轮不新增数据库字段，继续沿用 `transactions.account` 承担“账户 / 支付方式”语义。
- 原因：当前用户需求更偏输入效率与展示表达，不足以支撑一次数据库迁移和字段拆分。
- 影响：前端文案可以呈现为“支付方式”，但后端存储与类型字段名仍保持 `account`。
- 后续：如果未来需要同时表达“资金账户”和“支付渠道”，再评估拆字段与迁移。

## 3. 显示货币与语言彻底拆分

- 决策：`language` 只负责界面语言，`displayCurrency` 只负责金额展示格式。
- 原因：语言并不天然等于货币，同一语言环境下也可能希望用不同币种查看金额。
- 影响：前端所有金额格式化必须显式读取 `displayCurrency`，不能再通过语言推断。
- 约束：数据库仍统一存储 `amount_cents`，不做汇率换算，也不改变底层金额语义。

## 4. 历史建议项直接来源于交易记录，不新增建议表

- 决策：类别和支付方式建议项直接从已有交易中聚合获得，不单独维护建议表。
- 原因：现阶段目标是降低录入重复劳动，同时避免额外的数据一致性成本。
- 影响：后端需要提供聚合查询，并按最近使用时间、使用次数和字母顺序排序。
- 约束：建议项只能辅助输入，不能限制用户自由输入新值。

## 5. 建议输入优先采用原生 `input + datalist`

- 决策：本轮不引入重型组合框依赖，统一采用原生 `input + datalist`。
- 原因：需求核心是轻量建议而不是复杂搜索选择器；原生方案足以满足“可输入 + 可建议”。
- 影响：实现成本低、依赖少，也更容易在新增页和编辑弹窗复用。
- 后续：若未来需要分组、键盘导航增强、标签管理等复杂能力，再考虑升级组件方案。

## 6. 列表分页由后端负责

- 决策：列表页改为后端分页，前端通过 `list_transactions(limit, offset)` + `count_transactions()`
  获取页面数据与总数。
- 原因：随着数据量增加，前端一次性加载全量记录会影响性能，也会让筛选与分页逻辑变重。
- 影响：筛选应用或重置时需要回到第一页；页大小当前固定为 `20`。
- 后续：如未来需要无限滚动或自定义页大小，可在现有后端分页基础上扩展。

## 7. 看板时间范围采用“草稿值 / 已应用值”双状态

- 决策：用户修改时间范围时不立即刷新统计，只有点击 `Apply` 才更新。
- 原因：图表和聚合请求成本高于普通表单输入，草稿态更符合分析型页面的使用心智。
- 影响：看板页首次进入默认应用“近 30 天”；类别图与支付方式图使用同一已应用范围。

## 8. 支付方式统计与类别统计保持相同口径

- 决策：新增 `stats_by_account`，默认按 `expense` 维度统计 Top 10，和现有 `stats_by_category` 对齐。
- 原因：这样能减少解释成本，也更利于比较“钱花在哪”和“通过什么方式花出去”。
- 影响：当前看板页展示的是“支付方式 Top 10”，但底层字段仍来自 `account`。

## 9. 打开目录与恢复备份统一由 Rust 侧执行

- 决策：目录打开、备份恢复和应用重启都由 Rust / Tauri 命令封装，前端只负责触发。
- 原因：这些能力依赖本地文件系统和桌面生命周期控制，放在后端更安全、更稳定，也更易校验路径边界。
- 影响：打开目录通过 `tauri-plugin-opener`；恢复备份仅允许选择备份目录中的 `.sqlite`
  文件；恢复前必须先生成一份 `pre-restore` 安全备份；恢复成功后由 `restart_app` 请求应用重启。

## 10. 生成文件允许随 Tauri 能力变化一起提交

- 决策：当命令、插件或 capability 发生变化时，`src-tauri/gen/schemas/*`
  的生成文件应作为同一改动的一部分提交。
- 原因：这些文件反映桌面端实际能力声明，遗漏会导致运行时或构建时不一致。
- 约束：生成文件不手改，只接受由配置变化带来的更新结果。

## 11. 列表页已应用筛选以 URL query 为准

- 决策：列表页把已应用筛选和页码写入 URL query，URL 作为页面恢复与钻取跳转的单一来源。
- 原因：这样可以支持刷新恢复、浏览器前进后退，以及从看板图表直接跳转到带筛选的列表页。
- 影响：前端保留筛选草稿值，但只有点击 `Apply` 后才写入 URL 并触发数据刷新。

## 12. 删除采用“先隐藏、短暂可撤销、再真正删库”

- 决策：列表页删除改为先从界面隐藏，给出 5 秒撤销窗口，超时后再调用真正的数据库删除。
- 原因：相比二次确认弹窗，这种交互更高效，也更适合桌面端高频操作。
- 影响：当前实现不引入回收站，也不跨页面持久化撤销状态；离开页面时会尽快提交待删除项。
